# Security Baseline

Backend implementation must wait for audit and tests. Required: httpOnly browser sessions, CSRF, rate limiting, tenant isolation, RBAC authorizer, audit events, and separate service/scoped tokens for future AI tooling.
